import { TypeDescriptionContext } from './IComponentRendererAdapter_combined';
export declare class IgcComponentRendererContainerComponent {
    private _attrIgnoreList;
    private _observer;
    static fromElement(ele: Element): any;
    private collectExtraSlots;
    private ensureExtraSlots;
    private extraSlotNames;
    private extraSlotNamesMap;
    element: Element;
    private shadowMode;
    private childContentMode;
    private listeners;
    private _currentRoot;
    createObject(t: any, container: any, context: TypeDescriptionContext): any;
    replaceRootItem(t: any, deferAttach: boolean, continueActions: (resumeRequired: boolean) => void): void;
    clearContainer(continueActions: (resumeRequired: boolean) => void): void;
    getRootObject(): any;
    static isEvent(obj: any): boolean;
}
